@model Actor

@{
    ViewData["Title"] = "Add a new actor";
}

<div>
    <div class="col-md-4 offset-4">
        <p>
            <h1>
                Details for @Model.FullName
            </h1>
        </p>
        <div class="row-md-4">
            <div>
                    <div class="row justify-content-center">
                        <img class="border-info rounded-circle" style="max-width: 150px;" id="ProfilePicturePreview" />
                    </div>
                    <div class="">
                        <label asp-for="ProfilePictureURL" class="form-label"></label>
                        <input asp-for="ProfilePictureURL" class="form-control" readonly />
                        <span asp-validation-for="ProfilePictureURL" class="text-danger"></span>
                    </div>
                    <div>
                        <label asp-for="FullName" class="form-label"></label>
                    <input asp-for="FullName" class="form-control" readonly />
                        <span asp-validation-for="FullName" class="text-danger"></span>
                    </div>
                    <div>
                        <label asp-for="Bio" class="form-label"></label>
                    <input asp-for="Bio" class="form-control" readonly />
                        <span asp-validation-for="Bio" class="text-danger"></span>
                    </div>

                    <div class="">
                    <a class="btn btn-outline-success float-right" asp-action="Edit" asp-route-id="@Model.Id" style="margin: 10px 0 0 0;">Edit</a>
                    <a class="btn btn-outline-secondary" asp-action="Index" style="margin: 10px 0 0 0;">Show</a>
                    </div>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    <script>
        $(document).ready(function () {
            var output = document.getElementById('ProfilePicturePreview');
            output.src = $("#ProfilePictureURL").val();
        })
    </script>
}
